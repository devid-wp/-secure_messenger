{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability set for the Secure Messenger main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
